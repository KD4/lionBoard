<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.github.lionboard.repository.PostRepository">

    <select id="findByPage" resultType="com.github.lionboard.model.Post">
        SELECT *, uesrs.name userName
        FROM POST_TB posts
        LEFT JOIN POST_FIGURE_TB figures ON posts.userId = figures.id
        LEFT JOIN USERINFO_TB users ON posts.userId = users.id
        LEFT JOIN
        ORDER BY posts.postNum DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>


</mapper>

